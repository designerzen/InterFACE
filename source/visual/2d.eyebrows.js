import { clamp, TAU } from "../maths/maths.js"
import PALETTE, { DEFAULT_COLOURS } from "../settings/palette.js"


const DEFAULT_OPTIONS = {

}

/**
 * Draw an eyebrow onto the Canvas
 * @param {*} keypoints 
 * @param {*} isLeft 
 * @param {*} options 
 */
export const drawEyebrows = (keypoints, isLeft=true, options=DEFAULT_OPTIONS) => {

}
